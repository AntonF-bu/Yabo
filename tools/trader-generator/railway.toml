[build]
dockerfilePath = "tools/trader-generator/Dockerfile"
watchPatterns = ["tools/trader-generator/**"]

[deploy]
healthcheckPath = "/status"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
# Separate from behavioral-mirror — no shared config
internalPort = 8000
